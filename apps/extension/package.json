{
  "name": "@designbridge/app-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@designbridge/capture-core": "workspace:*",
    "@designbridge/client": "workspace:*",
    "@designbridge/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
